import React, { Fragment } from "react";

import Label from "../../components/Label";

const New = () => {
  return (
    <Fragment>
      <Label>new 페이지입니다.</Label>
    </Fragment>
  );
};

export default New;
